{
  "name": "@cms-be-all/shared",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no lint configured for shared package' && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
